{
  "$comment": "Read by scripts/check-e2e-results.mjs; see tests/golden/README.md. A pending entry is a ratchet: the checker fails once its blocker is gone and the entry is still here.",
  "fixturesRoot": "crates/urollup-core/tests/fixtures",
  "goldenRoot": "tests/golden/e2e",
  "pendingFixtures": {
    "bead": "uro-obx5",
    "unblock": "the sanitized fixture corpus lands under fixturesRoot; then delete pendingFixtures"
  },
  "commands": [
    {
      "name": "report",
      "args": ["report", "--all", "--format", "json", "--timezone", "UTC"],
      "compare": "all",
      "pending": {
        "bead": "uro-d135",
        "unblock": "urollup report prints JSON; then delete this pending entry and fix the report extractor"
      }
    },
    {
      "name": "daily",
      "args": ["daily", "--all", "--format", "json", "--timezone", "UTC"],
      "compare": ["requests", "ownership", "tokens"],
      "pending": {
        "bead": "uro-d135",
        "unblock": "urollup daily prints JSON; then delete this pending entry and fix the rows extractor"
      }
    },
    {
      "name": "sessions",
      "args": ["sessions", "--all", "--format", "json", "--timezone", "UTC"],
      "compare": ["requests", "ownership", "tokens"],
      "pending": {
        "bead": "uro-d135",
        "unblock": "urollup sessions prints JSON; then delete this pending entry and fix the rows extractor"
      }
    }
  ]
}
